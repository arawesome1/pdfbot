streamlit  # Ensure this is compatible with your app
langchain  # Adjust to a compatible version
langchain_community # Ensure compatibility with langchain
langchain_chroma  # No specific version, allows for the latest
chromadb# Align with other dependencies
pydantic  # Ensure compatible version
protobuf  # Align with grpc requirements
dataclasses-json # Ensure compatibility
Pillow  # Specified version to avoid issues
pip  # Ensure you have the latest pip

